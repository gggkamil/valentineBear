import React from 'react'
import ReactDOM from 'react-dom/client'

import ValentineComponent from './ValentineComponent.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ValentineComponent />
  </React.StrictMode>,
)
